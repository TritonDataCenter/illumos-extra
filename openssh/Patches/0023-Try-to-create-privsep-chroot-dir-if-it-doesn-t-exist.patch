From 65628dfce7386f10df00ac916d7edbdcfcbf3e21 Mon Sep 17 00:00:00 2001
From: Alex Wilson <alex.wilson@joyent.com>
Date: Wed, 5 Aug 2015 12:25:15 -0700
Subject: [PATCH 23/32] Try to create privsep chroot dir if it doesn't exist
 yet

---
 sshd.c | 7 +++++--
 1 file changed, 5 insertions(+), 2 deletions(-)

diff --git a/sshd.c b/sshd.c
index 20ae24a..6324d19 100644
--- a/sshd.c
+++ b/sshd.c
@@ -1936,8 +1936,11 @@ main(int ac, char **av)
 
 		if ((stat(_PATH_PRIVSEP_CHROOT_DIR, &st) == -1) ||
 		    (S_ISDIR(st.st_mode) == 0))
-			fatal("Missing privilege separation directory: %s",
-			    _PATH_PRIVSEP_CHROOT_DIR);
+			if (mkdir(_PATH_PRIVSEP_CHROOT_DIR, 0755))
+				fatal("Failed to create privilege separation "
+				    "directory %s: %s",
+				    _PATH_PRIVSEP_CHROOT_DIR,
+				    strerror(errno));
 
 #ifdef HAVE_CYGWIN
 		if (check_ntsec(_PATH_PRIVSEP_CHROOT_DIR) &&
-- 
2.3.2 (Apple Git-55)

